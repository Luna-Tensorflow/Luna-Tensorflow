
# Each layer should implement such interface
# interface Layer:
#       typetag :: a
#       shape :: List Int # shape of layer's output
#       forward :: Operation a -> Operation a # forward should return layers output after pipeing the input through all of its parents
#       trainableVariables :: List Variable a # should return its own and its parents trainable variables (without duplicates)
#       outputType :: typetag
